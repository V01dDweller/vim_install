# file: molecule/default/converge.yml
---
- name: Converge
  hosts: all
  gather_facts: false
  roles:
    - role: vim_install
      become: true
      tags: vim
...
# vim: ft=ansible syntax=yaml.ansible:
